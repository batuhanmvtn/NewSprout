# NewSprout
Small Visual Studio application for foundation which users add how many saplings they planted

Note: The database file location to the database is set to the PC it was created at. The Datasource setting must be redone if you want to see how the project works.
